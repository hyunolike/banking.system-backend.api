## 📌 작업 내용

<!-- 무엇을 왜 바꿨는지 적습니다. -->

## 🔗 관련 이슈

<!-- closes #이슈번호 -->

## ✅ 체크리스트

- [ ] `./gradlew build` 가 로컬에서 통과한다
- [ ] 변경한 동작에 대한 테스트를 추가했다
- [ ] 새 설정값(환경변수)을 추가했다면 `application.yml` 기본값과 README 표에 반영했다
- [ ] DB 스키마를 바꿨다면 `schema.sql` 과 `db/migration/` 스크립트를 **모두** 갱신했다
- [ ] 자격증명 · 토큰 · 키가 코드나 설정 파일에 포함되지 않았다
- [ ] 인증 · 인가 규칙을 바꿨다면 어떤 경로가 열리고 닫히는지 아래에 적었다

## 💬 리뷰 포인트

<!-- 특히 봐주었으면 하는 부분, 고민했지만 확신이 없는 선택 등 -->

## ⚠️ 배포 시 주의

<!--
머지 후 사람이 직접 해야 하는 일이 있다면 적습니다. 없으면 "없음".
예) 마이그레이션 스크립트 실행 순서, 새로 추가된 환경변수, 배포 순서 의존성
-->
없음
